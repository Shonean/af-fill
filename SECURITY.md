# 安全策略

## 支持的版本

仅最新 release 与 `main` 分支接受安全修复。

## 报告漏洞

**请勿开公开 Issue。** 走 GitHub 私密通道：

https://github.com/Shonean/af-fill/security/advisories/new

请附：影响范围（引擎 / 驱动 / 工作台）、复现步骤、影响（如数据泄露 / 脚本被页面劫持 / 注入绕过）。会尽快回复。

## 特别提醒

- **不要**在 Issue / PR / 讨论里粘贴真实档案、身份证、API Key 或含隐私的截图
- LLM API Key 只应保存在本机（Tampermonkey 存储 / `data/config.json`）；一旦泄露请立即到对应服务商吊销
- 怀疑某个招聘网站利用表单字段实施**脚本注入攻击**（例如把 payload 放进 label/placeholder 诱导填充）也欢迎上报
- 本项目不内置统计/遥测；若发现任何非用户显式配置的网络请求，视为安全缺陷

## 已知边界

- 脚本运行在页面上下文，天然可见你填写的内容；请只在自己信任的设备与浏览器配置中使用
- 目标网站的 CSP 可能强制 UI 降级；这不属于安全漏洞
- 敏感字段（身份证等）默认不自动填、LLM 默认脱敏，是设计保证；相关绕过视为缺陷
